#include<stdio.h>

int main()
{
    float a;  // a is a variable of type int which contain some data
    printf("Enter A Number");
    scanf("%f",&a);     // & -> address of operator 
    printf("A is = %f",a);
    return 0;
}
